def predict_all(models, ds):
    # Return empty dict of predictions
    return {k: [] for k in models}

def load_models_and_predict(ds, cfg):
    # Stub for loading fitted models and producing predictions
    return {"rf": []}
